{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf frontend/dist frontend/node_modules && cd frontend && npm install && node check-env.js && NODE_ENV=production npm run build && cd ../backend && npm install --production"
  },
  "deploy": {
    "startCommand": "cd backend && NODE_ENV=production npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
