{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && rm -rf dist node_modules/.vite && npm install && node check-env.js && NODE_ENV=production VITE_API_URL=https://spinelineweb-production.up.railway.app npm run build && cd ../backend && npm install --production"
  },
  "deploy": {
    "startCommand": "cd backend && NODE_ENV=production npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
